<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Payment QR Code</title>
    </head>
    <body>
        <h2>Payment QR Code</h2>
        <!-- <?php
        // Retrieve the file path from the query parameters
        $filePath = isset($_GET['file_path']) ? $_GET['file_path'] : '';

        // Check if the file path is provided
        if (!empty($filePath)) {
            // Display the image
            echo '<img src="' . $filePath . '" alt="QR Code">';
        } else {
            echo '<p>Invalid file path or file not found.</p>';
        }
        ?> -->
        <img src="img/QRCode/qrcode.png" alt="">
    </body>
</html>
